# GAPAGO 의존성 — 직접 의존하는 패키지만 나열 (버전 미고정)
#
#   설치: pip install -r requirements.txt
#
# 이 목록은 실제 설치·서버 기동까지 검증했습니다 (2026-08).
# 기본은 CPU 전용 PyTorch (~200MB). GPU 리랭킹은 lock 파일 쪽을 참고하세요.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi
uvicorn
langchain
langchain-anthropic
langchain_groq
langchain-aws
langchain-community
langchain-core
# langchain-google-vertexai
langchain-google-genai
langchain-openai
langchain-text-splitters
langgraph
langgraph-checkpoint
langgraph-prebuilt
langsmith
openai
anthropic
google-genai
tiktoken
rank-bm25
arxiv
tavily
beautifulsoup4
feedparser
PyMuPDF
pymupdf4llm
faiss-cpu
sentence-transformers
# 아래 셋은 직접 import 한다 (gapago/agents/retrieval_agent.py, gapago/core/llm.py).
# 지금까지는 sentence-transformers 가 전이로 끌어와 우연히 동작했다.
numpy
torch
transformers

# ── ONNX 가속 (선택) ─────────────────────────────────────────
# retrieval_agent.py 는 sentence-transformers 의 backend="onnx" 를 시도하고
# 실패하면 PyTorch 로 폴백한다. 따라서 없어도 동작한다.
#
# 기본 설치에서 제외한 이유: optimum 이 transformers<5 를 요구하는데 이 코드는
# transformers 5.x 에서 동작한다. 함께 두면 pip 이
# optimum 버전을 끝없이 되짚는 백트래킹에 빠진다.
# 실제로 동작했던 HPC 환경에도 두 패키지는 없었다.
#
# CPU 추론을 가속하려면 transformers<5 로 내린 뒤 아래를 설치하라:
#   pip install "transformers<5" onnxruntime "optimum[onnxruntime]"
requests
python-dotenv
pydantic
pydantic-settings
pycryptodome

# ── 평가 전용 (선택) ─────────────────────────────────────────
# evaluation/ 과 scripts/evaluate.py 에서 사용. 없으면 해당 도구만 제한된다.
scikit-learn
