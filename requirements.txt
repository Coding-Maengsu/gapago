# GAPAGO 의존성 — 직접 의존하는 패키지만 나열 (버전 미고정)
#
#   설치: pip install -r requirements.txt
#
# 이 목록은 실제 설치·서버 기동까지 검증했습니다 (2026-08).
# 기본은 CPU 전용 PyTorch (~200MB). GPU 리랭킹은 lock 파일 쪽을 참고하세요.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi
uvicorn
langchain
langchain-anthropic
langchain_groq
langchain-aws
langchain-community
langchain-core
# langchain-google-vertexai
langchain-google-genai
langchain-openai
langchain-text-splitters
langgraph
langgraph-checkpoint
langgraph-prebuilt
langsmith
openai
anthropic
google-genai
tiktoken
rank-bm25
arxiv
tavily
beautifulsoup4
feedparser
PyMuPDF
pymupdf4llm
faiss-cpu
sentence-transformers
# 아래 셋은 직접 import 한다 (gapago/agents/retrieval_agent.py, gapago/core/llm.py).
# 지금까지는 sentence-transformers 가 전이로 끌어와 우연히 동작했다.
numpy
torch
transformers<5   # optimum[onnxruntime] 요구사항

# ── ONNX 가속 ───────────────────────────────────────────────────
# retrieval_agent.py 가 sentence-transformers 의 backend="onnx" 로 CPU 추론을
# 가속한다 (기술보고서 2.4). 실패 시 PyTorch 로 폴백한다.
#
# optimum 이 transformers<5 를 요구하므로 아래 핀이 필요하다.
# 이 핀을 풀면 pip 이 optimum 버전을 끝없이 되짚는 백트래킹에 빠진다.
onnxruntime
optimum[onnxruntime]

requests
python-dotenv
pydantic
pydantic-settings
pycryptodome

# ── 평가 · 개발 전용 (선택) ───────────────────────────────────
# evaluation/ 에서 사용. 없으면 해당 도구만 제한되고 서버는 정상 동작한다.
scikit-learn
pytest
