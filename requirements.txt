# GAPAGO 의존성 — 직접 의존하는 패키지만 나열 (버전 미고정)
#
#   설치: pip install -r requirements.txt
#
# 정확한 재현이 필요하면 requirements-lock.txt (핀 고정) 를 사용하세요.
# 기본은 CPU 전용 PyTorch (~200MB). GPU 리랭킹은 lock 파일 쪽을 참고하세요.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi
uvicorn
langchain
langchain-anthropic
langchain_groq
langchain-aws
langchain-community
langchain-core
# langchain-google-vertexai
langchain-google-genai
langchain-openai
langchain-text-splitters
langgraph
langgraph-checkpoint
langgraph-prebuilt
langsmith
openai
anthropic
google-genai
tiktoken
rank-bm25
arxiv
tavily
beautifulsoup4
feedparser
PyMuPDF
pymupdf4llm
faiss-cpu
sentence-transformers
# 아래 셋은 직접 import 한다 (agents/retrieval_agent.py, core/llm.py).
# 지금까지는 sentence-transformers 가 전이로 끌어와 우연히 동작했다.
numpy
torch
transformers
onnxruntime
optimum[onnxruntime]
requests
python-dotenv
pydantic
pydantic-settings
pycryptodome

# ── 평가 전용 (선택) ─────────────────────────────────────────
# evaluation/ 과 scripts/evaluate.py 에서 사용. 없으면 해당 도구만 제한된다.
scikit-learn
